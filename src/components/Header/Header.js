import React from 'react';
import NavLinks from '../NavLinks/NavLinks';
/* 
This component is for making together all the links
*/

const Header = () => {
    return (
        <div>
            <NavLinks></NavLinks>
        </div>
    );
};

export default Header;